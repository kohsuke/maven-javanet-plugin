<?xml version="1.0"?>
<document>
<body>
<section name="Downloading the plugin">
  <p>The easiest way to install the plugin is with Maven itself. Issue a command like the following:</p>
 
<source>maven plugin:download \
  -Dmaven.repo.remote=https://maven-repository.dev.java.net/nonav/repository \
  -DgroupId=maven-javanet-plugin \
  -DartifactId=maven-javanet-plugin \
  -Dversion=1.2</source>

  <p>You'll need to run this command as a user with write access to <code>$MAVEN_HOME/plugins</code>.</p>

  <p>Alternatively, you can install the plugin manually - Firstly download the <code>jar</code> from
  <a href="https://maven-javanet-plugin.dev.java.net/servlets/ProjectDocumentList">the download page</a> and 
  place it into your <code>$MAVEN_HOME/plugins</code> directory. You may need to manually delete your
  plugin cache (usually <code>~/.maven/cache</code>) in order for the new version to be deployed properly.</p>

</section>

<section name="Entering your Java.net project details">
	<p>Regardless of the installation method you choose, you'll need to follow 
  <a href="https://javanettasks.dev.java.net/nonav/maven/config.html">this instruction</a> to 
  set up your java.net account information in the <code>~/.java.net</code> file.</p>
</section>

</body>
</document>
